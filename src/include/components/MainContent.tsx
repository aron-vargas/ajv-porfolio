import React from 'react';

const MainContent = () => {
	return (<div className="col-xs-12 col-sm-9 col-md-8 body">
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	<p>There are also default key bindings for column selection on macOS and Windows, but not on Linux.</p>
	</div>);
}

export default MainContent;